# 香料 3D 展示 · 部署与使用指南

## 项目结构

```
香料3D展示/
├── bohe/          薄荷页 + 压缩模型 (0.77MB)
├── jinyinhua/     金银花页 + 压缩模型 (0.52MB)
├── midiexiang/    迷迭香页 + 压缩模型 (0.81MB)
├── shared/        自托管 model-viewer + Draco 解码器 + 二维码库
├── qr/            本地二维码工具页
├── 部署指南.md     本文件
├── .skills/       taste-skill 设计技能（开发参考，无需部署）
└── .tools/        构建/验证脚本（无需部署）
```

## 为什么打开快

- 模型从 30-43MB 压缩到 0.5-0.8MB（gltf-transform 减面 + WebP 纹理 + Draco 几何压缩）
- model-viewer 与 Draco 解码器全部自托管，不依赖 unpkg / gstatic（这两个 CDN 国内不可达）
- 双通道加载：主通道 4 秒无进展自动切换 jsDelivr CDN 备用通道
- 实测本地加载 0.4-1.0 秒完成

## GitHub Pages 部署步骤

1. 在 GitHub 建仓库（例如 xiangliao），上传 bohe / jinyinhua / midiexiang / shared / qr 五个目录到仓库根目录（.tools 与 .skills 可不上传）
2. 仓库 Settings → Pages → Source 选择 main 分支 / (root)，保存
3. 等 1-2 分钟生效，访问 https://用户名.github.io/仓库名/bohe/ 确认
4. 本地双击打开 qr/index.html，把地址栏改成线上地址，切换植物标签生成对应二维码，下载 PNG 或直接投屏
5. 微信扫码即可打开；页面无任何外部依赖，微信内置浏览器可秒开

命令行上传（在 香料3D展示 目录执行）:

```bash
git init
git add bohe jinyinhua midiexiang shared qr
git commit -m "feat: 香料3D展示三个页面"
git branch -M main
git remote add origin https://github.com/用户名/仓库名.git
git push -u origin main
```

## 二维码工具页

- 纯本地生成（qrcode-generator 已自托管），不依赖 goqr.me，离线可用
- 三个植物快捷标签 + 自定义地址 + 下载 PNG + 复制链接
- 自动记住上次使用的地址（localStorage）
## 更新模型或页面

- 修改页面后重新 push，Pages 会自动更新
- 更换模型：用 .tools 里已装的 gltf-transform 流程压缩后，替换对应目录 models/*.glb
- 三个页面的加载提示里有模型体积文案（约 0.5 / 0.8 MB），换模型后记得同步修改

## 注意事项

- 页面里的备用通道 FALLBACK 地址当前指向 github.com/ruijiangyoumeile/xiangliao；如果部署到别的仓库，请把三个页面 script 里的 FALLBACK 常量改成自己的仓库地址
- GitHub Pages 对单文件 100MB 限制，本项目模型都小于 1MB，无压力
- 手机本地局域网测试：在 香料3D展示 目录运行 python -m http.server 8899，手机访问 http://电脑局域网IP:8899/bohe/
- AR 功能需要 HTTPS 环境（GitHub Pages 天然满足），iOS 使用 Quick Look，Android 使用 Scene Viewer